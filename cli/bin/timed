#! /usr/bin/env node

const minimist = require("minimist");
const ops = minimist(process.argv.slice(2));

const [value] = ops._;
const date = new Date(value).toLocaleDateString('AU');
console.info({date});
