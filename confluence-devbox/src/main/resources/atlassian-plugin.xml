<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="confluence-devbox"/>
    
    <!-- add our web resources -->
    <web-resource key="confluence-devbox-resources" name="Confluence Devbox Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="confluence-devbox.css" location="/css/confluence-devbox.css"/>
        <resource type="download" name="confluence-devbox.js" location="/js/confluence-devbox.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>confluence-devbox</context>
    </web-resource>

    <rest key="confluence-devbox-rest-resources"
          name="Confluence Devbox Rest Resources"
          path="/devbox"
          version="1.0">
        <package>org.viqueen.devbox.resources</package>
    </rest>

</atlassian-plugin>